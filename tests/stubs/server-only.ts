// Test stub. In Next.js, "server-only" is a build-time guard that throws
// if a server module is imported from a client component. It has no
// runtime behaviour, so tests resolve it to an empty module.
export {};
